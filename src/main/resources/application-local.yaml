matchuri:
  seed:
    enabled: false
    sample-members-enabled: true

spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false

logging:
  level:
    org.hibernate.SQL: info
