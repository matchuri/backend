spring:
  application:
    name: backend
  docker:
    compose:
      lifecycle-management: start-only
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

matchuri:
  test: ${MATCHURI_KEY_TEST}
